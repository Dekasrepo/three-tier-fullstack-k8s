apiVersion: v1
kind: ConfigMap
metadata:
  name: user-app-config
  namespace: user-app
data:
  APP_NAME: "User Management System"
  APP_VERSION: "1.0.0"
  ENVIRONMENT: "production"
  MAX_USERS: "100"
  DEFAULT_ROLE: "user"
  LOG_LEVEL: "info"